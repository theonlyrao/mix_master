<h1>Playlist <%= @playlist.name %></h1>

<h3>Songs in this playlist</h3>
<ul>
<%= @playlist.songs.each do |song| %>
  <li><%= link_to "#{song.title}", song_path(song.id) %></li>
<% end %>
</ul>

<p><%= link_to "Edit", edit_playlist_path(@playlist.id) %>
